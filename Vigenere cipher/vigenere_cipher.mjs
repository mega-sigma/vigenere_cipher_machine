import {
    exportJsAlphabet as strAlphabet, 
    currentTxtAlphabet as alphabet
} from '../universal_alphabet/txt_to_js_transformation.mjs'

const vigenereCipher = function (input, encryptOrDecrypt, key, output = []) {

}

const output = vigenereCipher()